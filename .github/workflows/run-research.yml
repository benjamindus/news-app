name: Run Research

on:
  workflow_dispatch:

jobs:
  research:
    runs-on: self-hosted
    timeout-minutes: 15
    steps:
      - uses: actions/checkout@v4

      - name: Run morning briefing update
        run: bash update-briefing.sh
